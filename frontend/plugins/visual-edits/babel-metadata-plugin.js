// babel-metadata-plugin.js (DISABLED)
// This plugin was disabled to prevent "Maximum call stack size exceeded"
// on large component files like CheckInPage.jsx.

module.exports = function () {
  return {
    name: "visual-edits-metadata-disabled",
    visitor: {},
  };
};